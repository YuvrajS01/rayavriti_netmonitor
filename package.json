{
  "name": "rayavriti-netmonitor",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x || 22.x || 26.x"
  },
  "description": "Rayavriti NetMonitor — production-grade network monitoring platform",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && npm run build -w client",
    "build:server": "npm run build -w server",
    "build:client": "npm run build -w client",
    "typecheck": "npm run typecheck -w server",
    "start": "npm run start -w server",
    "start:prod": "NODE_ENV=production node server/dist/index.js",
    "simulate": "npx ts-node simulator/run.ts"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  }
}

