interface Item { label: string; value: number; color?: string; detail?: string; }
export default function Treemap({ items, title = 'Bandwidth distribution' }: { items: Item[]; title?: string }) {
  const total = items.reduce((sum, item) => sum + item.value, 0) || 1;
  return <section className="bg-surface-container-low rounded-lg border border-outline-variant/20 p-5 h-full"><h3 className="text-sm font-headline font-semibold uppercase tracking-wide mb-4">{title}</h3><div className="flex h-[230px] gap-1.5" role="img" aria-label={title}>{items.length ? items.map((item, index) => <div key={item.label} className="relative min-w-10 rounded-md overflow-hidden group transition-all hover:brightness-125" style={{ flex: Math.max(item.value / total, .08), background: item.color || `var(--color-chart-${index % 8 + 1})` }} title={`${item.label}: ${item.detail || item.value}`}><div className="absolute inset-0 bg-surface-dim/20 opacity-0 group-hover:opacity-100 transition-opacity"/><div className="absolute bottom-0 left-0 right-0 p-2 bg-surface-dim/50"><span className="block text-[10px] font-semibold text-on-surface truncate">{item.label}</span><span className="block text-[9px] text-on-surface-variant">{Math.round(item.value / total * 100)}%</span></div></div>) : <p className="m-auto text-xs text-on-surface-variant">No distribution data</p>}</div></section>;
}
