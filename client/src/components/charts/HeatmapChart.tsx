interface HeatmapRow { label: string; values: Array<number | null>; }
interface Props { rows: HeatmapRow[]; columns?: string[]; onCellClick?: (row: number, column: number) => void; }
export default function HeatmapChart({ rows, columns, onCellClick }: Props) {
  return <div className="overflow-x-auto" role="img" aria-label="Heatmap chart"><div className="min-w-[420px] space-y-1.5">{columns && <div className="grid grid-cols-[100px_repeat(12,minmax(0,1fr))] gap-1 text-[9px] text-on-surface-variant">{['', ...columns.slice(0, 12)].map((column, index) => <span key={`${column}-${index}`} className="truncate text-center">{column}</span>)}</div>}{rows.map((row, rowIndex) => <div key={row.label} className="grid grid-cols-[100px_repeat(12,minmax(0,1fr))] gap-1 items-center"><span className="text-xs truncate text-on-surface-variant">{row.label}</span>{row.values.slice(0, 12).map((value, columnIndex) => <button key={columnIndex} type="button" onClick={() => onCellClick?.(rowIndex, columnIndex)} className="h-5 rounded-sm transition-transform hover:scale-110" style={{ background: value == null ? 'var(--color-outline-variant)' : `color-mix(in oklch, var(--color-success) ${Math.max(12, Math.min(100, value))}%, var(--color-error))` }} aria-label={`${row.label}, ${columns?.[columnIndex] || columnIndex}: ${value ?? 'no data'}`} />)}</div>)}</div></div>;
}
